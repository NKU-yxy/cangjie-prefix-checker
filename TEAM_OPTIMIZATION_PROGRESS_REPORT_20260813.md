# 初赛代码至今的优化与正确性全景报告

> 队内同步版 · 面向第一次接触本项目的读者
>
> 报告日期：2026-08-13
>
> 仓库：project3230617-388044
>
> 本文只保存在本地工作区，不加入 Git、不推送远程

---

## 1. 一页结论

### 1.1 我们从哪里出发

- 初赛提交锁定基点：**b40791c**
- 官网最后提交时间：**2026-08-05 02:59:28**
- 官网结果：**AC，25.00 分**
- 官网公开 50 个错误样例：**50/50 全部在规定的首错 token 拒绝**

这里的 AC 表示 Accepted，即官方测试认为提交满足当时公开测试要求。它不代表代码已经覆盖仓颉语言的全部情况，也不代表隐藏输入一定没有遗漏。

### 1.2 已经正式保留的优化

目前生产代码中保留了三项经过完整正确性门禁和同轮 A/B/A 性能验证的通用优化：

1. **线性 malformed declaration 扫描**
   - 将反复执行的整段正则扫描改为一次线性扫描；
   - 同轮 SUM 改善 **5.358%**。

2. **nominal 声明索引复用**
   - class/interface 等声明只收集一次，在同一次检查中复用；
   - 同轮 SUM 改善 **8.204%**。

3. **generation 级 DeclarationSnapshot**
   - 在原有 model 重建时机生成声明快照，多个语义检查共享；
   - 同轮 SUM 从 **1646.111 ms** 降至 **1511.386 ms**，改善 **8.184%**。

第一轮前两项的同轮累计结果是：

- SUM：**1913.215 → 1643.670 ms**
- 改善：**14.089%**
- MEDIAN / P95 / MAX 分别改善：
  **13.516% / 16.689% / 16.143%**
- A1/A2 漂移：**0.333%**
- 46 个显著胜例，0 个显著败例

上述 14.089% 和后续 8.184% 是每项优化当时的独立验收结果，仍不能直接相加。为了给队伍一个“初赛代码到当前代码”的单一可比数字，2026-08-13 又在同一官方 AArch64 容器中补跑了 `b40791c → 68d780d → b40791c`：

- 初赛 A1/A2 逐例合成 control SUM：**1930.374 ms**
- 当前版本 SUM：**1531.869 ms**
- 直接累计改善：**20.644%**
- MEDIAN / P95 / MAX 改善：
  **20.908% / 23.595% / 24.973%**
- A1/A2 SUM 漂移：**1.147%**
- 47 个显著胜例、0 个显著败例，50 个样例均无正回退
- A1 / 当前 / A2 均为 **50/50**，所有正式 trial 全部正确

由于宿主从空闲进入持续负载时会降频，前两次未稳定的试跑出现约 21% 的 A1/A2 漂移，均按合同判 INVALID、没有纳入结论。最终数据来自先完成一整轮对称稳定化 A/B/A 后、在同一容器和同一组二进制上执行的正式 A/B/A；正式 A1/A2 已满足 3% 漂移门槛。

### 1.3 正确性后来发生了什么

更全面的 373 例语料发现了公开 50 例没有覆盖到的两个通用错误：

- lambda 块体后继续 IIFE 调用时，旧代码可能提前拒绝；
- constructor / method / index 后缀链尚未完成时，旧代码可能提前做最终类型比较。

它们是“可继续补全的合法前缀被过早拒绝”，属于 false reject。

我们用两个独立提交修复：

- **6382994**：修复 lambda/IIFE 前缀；
- **c35afae**：修复 call/member/index 后缀链。

修复后：

- 官方公开精确首错：**50/50**
- authoritative 综合语料：**219/219**
- 官方语义语料：**45/45**
- 项目语料：**57/57**
- hidden fuzz：**144 × 5，0 失败**
- native fragment：**66 × 4 全过**
- native context：**7/7**
- ASan/UBSan：**全过**

### 1.4 当前推荐版本

| 用途 | 推荐检查点 | 原因 |
|---|---|---|
| 最精简、风险最低的最终生产代码锚点 | **c35afae** | 包含三项已接受优化和两项正确性修复，不含后续画像脚手架 |
| 继续做性能研究的 control | **68d780d** | 生产语义与 c35afae 相同，额外画像只在诊断编译开关打开时存在 |
| 当前本地 HEAD | **1fe3f04** | 生产文件与 68d780d 完全相同，只多了测试、结果和报告历史 |

如果现在就要生成最终提交包，最保守做法是从 **c35afae** 建一个干净提交分支，并在该 checkout 上重新跑一次完整官方 Linux AArch64 门禁。不要直接回到 8db0fbf 或 58f03c7，因为那两个版本还没有后来的两项正确性修复。

### 1.5 当前仍存在的已知问题

- 145 个 spec-pending 样例中有 40 个标签差异，但它们缺少独立赛事规范或 oracle，不能直接认定为代码错误；
- 9 个 scale 诊断样例中：
  - 4KB identifier；
  - 300 locals；
  仍会在 30 秒左右超时；
- 两个超时已经定位到 XGrammar/grammar 的增量状态膨胀，不是 native semantic 主体慢；
- 赛题官网已经关闭，所以优化后没有新的官方总分。当前只能给出严格的本地官方镜像验证结果，不能承诺隐藏榜分。

---

## 2. 先理解这个项目在做什么

这个程序不是一次性读取完整源码再给答案，而是一个“增量代码片段检查器”。

官方测试大致按下面的方式工作：

1. 把仓颉源码编码成很多 token；
2. 一次只把一个 token 对应的文本片段发送给 solution；
3. solution 每收到一个 token，就必须立即输出一行：
   - 1：截至当前前缀，仍可能补成合法程序；
   - 0：当前前缀已经出现不可恢复错误；
4. 对错误样例，solution 必须恰好在官方规定的第一个错误 token 输出 0。

因此，这个题目同时要求：

- 语法判断正确；
- 类型和语义判断正确；
- 不提前拒绝仍可补全的前缀；
- 不推迟已经确定的错误；
- 每个 token 都立刻响应；
- 每次启动和每次检查都尽可能快。

这也是为什么“最终完整程序判对”仍然不够：如果官方要求在 token 32 首次拒绝，而我们在 token 31 提前拒绝或 token 33 才拒绝，都算错误。

---

## 3. 零基础术语表

### 3.1 token

token 可以理解为分词后的最小输入单位，例如关键字、标识符、括号或字符串的一部分。

本题的计时和正确性是按 token 逐步交互的，不是只看完整文件。

### 3.2 prefix，前缀

前缀是“源码从第一个字节开始，到当前已经收到的位置为止”的内容。

例如完整程序是：

    let x: Int64 = make().value

测试过程中可能依次检查：

    l
    let
    let x:
    let x: Int64 = make(
    let x: Int64 = make()
    let x: Int64 = make().value

中间前缀不完整并不等于错误。检查器必须判断它“是否还可能继续补成合法程序”。

### 3.3 syntax，语法

语法检查关注代码形状是否符合 grammar，例如：

- 括号是否合理；
- if、for、func 的结构是否可能合法；
- token 顺序是否符合语言规则。

项目里的 syntax 路径主要由 XGrammar 完成。

### 3.4 semantic，语义

语义检查关注代码的含义，例如：

- 变量是否存在；
- let 是否被再次赋值；
- 参数数量是否正确；
- Int64 是否被赋给 Bool；
- class 是否实现了 interface 要求的方法。

项目里的 semantic 路径主要由 native C++ 语义检查器完成。

### 3.5 grammar

grammar 是语法规则文件，可以理解为“什么 token 序列算合法程序”的形式化描述。

修改 grammar 的风险很高，因为它可能改变所有前缀的合法性，而不仅是某一个完整程序。

### 3.6 first error，精确首错

精确首错指“第一次输出错误的 token 位置必须与官方一致”。

三种情况的区别：

- 正确：官方 token 32 错，我们也在 token 32 首次输出 0；
- 提前拒绝：我们在 token 31 输出 0；
- 延迟拒绝：我们在 token 33 才输出 0。

后两种都不通过官方精确首错门禁。

### 3.7 false reject 与 false accept

- false reject：本来仍合法或仍可补全，却被错误拒绝；
- false accept：已经确定错误，却被错误放过。

本轮新增语料发现的两个 authoritative 问题都是 false reject。

### 3.8 oracle

oracle 是独立于当前实现的参考判定来源。

例如，另一个配套 parser/typechecker 对某个完整程序给出明确结论，并能确认错误位置，那么它可以作为独立证据。独立很重要：如果用我们自己的 grammar 给我们自己的测试标签背书，就会形成循环证明。

### 3.9 authoritative

authoritative 表示具有足够独立证据、可作为硬门禁的样例。

当前综合语料中有 219 个 authoritative 样例。任何新候选只要有一个失败，就必须停止性能测试。

### 3.10 spec-pending

spec-pending 表示测试设计看起来合理，但还缺少独立的官方规范或 oracle 证明。

它们适合发现差异和讨论语言含义，但不适合强迫生产代码迎合标签。当前有 145 个，其中 40 个与实现结果不同，这 40 个不能直接叫“40 个代码 bug”。

### 3.11 scale 样例

scale 样例是用于观察超长输入、复杂度和内存增长的压力测试。

它主要回答“输入变大后会不会非常慢、超时或爆内存”，不一定属于赛事正确性硬门禁。

### 3.12 baseline、control 与 candidate

- baseline：参考基线；
- control：本轮不修改的对照版本；
- candidate：本轮待验证的新版本。

正式性能结论只能比较同一轮、同一环境中的 control 和 candidate。

### 3.13 A/B/A

A/B/A 是一种降低环境噪声的测试顺序：

1. A1：先测旧版本；
2. B：再测候选版本；
3. A2：最后再次测旧版本。

如果 A1 和 A2 差很多，说明服务器负载或环境漂移了，这一轮数据不可信。

本项目每个阶段通常是：

- 1 次预热；
- 9 次正式实测；
- 每个样例每次都启动新进程；
- 每个 token 都即时交互。

收益落在 2% 到 5% 时，会升级到两轮完整的 1 次预热 + 21 次实测。

### 3.14 warmup，预热

预热是正式计数前先运行一次，用来减少首次加载动态库、文件缓存等偶然开销。预热结果不进入最终统计。

### 3.15 SUM、MEDIAN、P95 与 MAX

对 50 个官方样例：

- SUM：50 个样例耗时中位数的总和，主要性能指标；
- MEDIAN：把 50 个样例从快到慢排序后的中间值；
- P95：约 95% 样例不会超过的耗时，观察慢尾部；
- MAX：最慢单例。

不能只看 SUM。如果 SUM 变好，却让少数慢例严重变差，也不能正式接受。

### 3.16 drift，漂移

drift 是 A1 与 A2 的差异。

本合同规定漂移超过 3% 时整轮 INVALID，即本轮无效，不能据此接受或拒绝候选。

### 3.17 WIN、LOSS 与严重单例回退

单例变化太小可能只是噪声，所以我们为每例设置噪声阈值：

    max(1 ms, control × 3%)

- 快过这个阈值记为 WIN；
- 慢过这个阈值记为 LOSS。

如果某例同时慢超过 2 ms 且超过 8%，属于严重单例回退，即使总和好看也不能忽略。

### 3.18 profile，性能画像

profile 是在诊断构建中记录每个阶段花了多少时间、调用多少次、扫描多少字节。

它用于回答“哪里可能值得优化”，不直接证明优化已经生效。profile 自己有计时开销，因此不能拿 profile 构建的绝对时间当正式成绩。

### 3.19 regex，正则表达式

regex 是按文本模式查找声明或错误结构的工具。它开发方便，但反复对不断增长的完整源码执行时可能很慢。

本项目几项主要优化就是减少重复 regex 扫描，同时保留原正则作为正确性参照。

### 3.20 index 与 snapshot

- index：索引，把已经找到的声明位置和关键信息记录下来，后续直接查；
- snapshot：快照，在某个确定的重建时点保存一组声明事实。

它们类似于“先整理目录，后面按目录查”，避免每次都从第一页重新阅读全文。

### 3.21 generation 与 dirty

generation 可以理解为当前模型的一代缓存。

dirty 表示“源码变化可能使缓存过期，需要重建”。减少重建次数看起来很诱人，但风险很高：如果漏掉一次必要重建，就可能把首错从 token 32 推迟到 33。我们已经真实遇到过这种失败。

### 3.22 shadow path

shadow path 是只在测试构建中存在的旧实现对照路径。

新实现给出结果的同时，旧实现也运行；两者在每个前缀比较：

- 记录数量；
- 顺序；
- 捕获文本；
- 字节位置；
- 类型模型；
- 函数上下文；
- 错误消息。

一旦不同就立即断言失败。正式性能构建完全不包含 shadow 路径。

### 3.23 differential test，差分测试

差分测试是把同一输入交给两个实现，比较完整输出，而不只比较最终接受/拒绝。

当前性能候选会与已接受 control 比较逐 token transcript、首错、退出码和 stderr。

### 3.24 fuzz，模糊测试

fuzz 是按固定规则和随机种子生成大量程序变体，寻找人工样例未覆盖的问题。

固定 seed 能保证每次运行生成相同语料，结果可复现。

### 3.25 ASan、UBSan 与 TSan

- ASan：AddressSanitizer，检查越界访问、use-after-free 等内存错误；
- UBSan：UndefinedBehaviorSanitizer，检查未定义行为；
- TSan：ThreadSanitizer，检查线程数据竞争。

线程候选需要特别关注 TSan；涉及 offset、string_view 和缓存生命周期的候选需要 ASan/UBSan。

### 3.26 Docker、Linux AArch64 与 Apple 环境

- Docker：固定软件依赖和运行环境的容器；
- AArch64：64 位 ARM 架构，也是官方镜像目标；
- Apple Silicon 也是 ARM64，但 macOS、编译器、动态库和调度器都不同。

因此 Apple 本机绝对耗时不能当成官方服务器耗时。我们只认可同一个官方 Linux AArch64 镜像里的相对 A/B/A。

### 3.27 Git commit 与语义提交点

这两个“提交”不是一回事：

- Git commit：保存一版代码历史；
- 语义提交点：当前表达式已经完整，语义错误可以稳定报告的位置，例如顶层分号、换行或真正的函数体闭合。

lambda 修复改的是第二种“语义提交点”。

### 3.28 ACCEPTED、PROVISIONAL、NO PROVEN GAIN、REJECTED、INVALID

- ACCEPTED：正确性全过，性能也达到合同门槛，成为下一 control；
- PROVISIONAL：有改善，但证据或幅度不足以正式接受；
- NO PROVEN GAIN：变化小于 2%，无法证明不是噪声；
- REJECTED：正确性失败、明确性能回退或其他硬条件失败；
- INVALID：A1/A2 环境漂移太大，本轮数据无效。

REVERT 表示用 Git 生成反向提交，把候选生产代码撤销。回退不等于候选一定有 bug，也可能只是收益没有达到我们事先约定的门槛。

### 3.29 XGrammar

XGrammar 是本项目使用的增量语法匹配库。它读取 grammar，维护“当前 token 前缀是否仍可能合法”的内部状态。

它与 native semantic 的分工可以粗略理解为：

- XGrammar 负责代码形状；
- native semantic 负责变量、类型、成员、接口等含义。

### 3.30 cl100k 与 tokenizer

tokenizer 是把文本切分成 token 的编码器。cl100k_base 是本地测试使用的一套固定分词规则。

同一个源码可以：

- 逐字节输入；
- 按 cl100k token 输入；
- 随机分片输入；
- 按行输入；
- 整段输入。

不同分片不应改变语义结论。cl100k 只是输入切分工具，不是用来针对官方样例训练的模型。

### 3.31 protocol、stdout、stderr 与 exit code

- protocol：solution 与测试程序约定的输入输出格式；
- stdout：标准输出，本题只能按协议输出 0 或 1；
- stderr：标准错误输出，用于诊断，不能混入协议答案；
- exit code：进程退出状态，0 通常表示正常结束；
- flush：把尚在缓冲区的数据立即发送出去。

本题要求每个 token 的答案立即 flush，不能等多个 token 后批量输出。

### 3.32 transcript

transcript 是一次完整交互记录，包括每个 token 对应的每一行输出。

比较 transcript 比只比较“最终接受还是拒绝”严格得多，因为它能发现提前拒绝和延迟拒绝。

### 3.33 byte、offset、length 与 string_view

- byte：字节，源码位置的基础计量单位；
- offset：某段内容从源码起点算起的位置；
- length：该段内容的字节长度；
- string_view：不拥有内存、只指向另一段字符串的轻量视图。

source 字符串扩容时，旧指针或 string_view 可能失效。因此 DeclarationSnapshot 只长期保存 offset/length 和自有字符串，不保存指向可扩容 source 的视图。

### 3.34 Model、Context 与 FunctionContext

- Model：当前已知的全局函数、class、interface、字段、方法等语义模型；
- Context：题目提供的固定外部环境，例如预置类型和函数；
- FunctionContext：当前函数内的变量、不可变变量、返回类型、所属 class 和函数体范围。

优化这些结构时，最危险的问题是缓存过期或作用域错位。

### 3.35 O(n)、O(n²) 与 O(n³)

这是描述输入增长时耗时增长速度的记号。假设输入规模扩大 2 倍：

- O(n)：耗时约 2 倍；
- O(n²)：耗时约 4 倍；
- O(n³)：耗时约 8 倍。

300 locals 和超长 identifier 的 syntax 经验增长接近 O(n³)，所以小输入看起来正常，扩大后会突然非常慢。

### 3.36 hash、SHA-256 与 digest

hash 是文件内容的数字指纹。只要文件内容变化，SHA-256 通常也会变化。

我们用它锁定：

- Docker 镜像；
- 官方样例；
- 首错位置 registry；
- 测试 manifest；
- solution 二进制。

digest 是镜像或文件指纹的另一种常用称呼。它们用于证明 A/B 测试没有偷偷换资源。

### 3.37 harness

harness 是驱动 solution 的测试程序。它负责启动进程、逐 token 发送输入、读取响应、计时和核对结果。

harness 自己也需要时间，所以 harness 总墙钟通常大于 solution 内部计算时间。

### 3.38 lambda、IIFE 与 postfix

- lambda：匿名函数，例如把一段函数逻辑直接写在表达式里；
- IIFE：Immediately Invoked Function Expression，定义后立刻调用的函数表达式；
- postfix：写在表达式后面、继续改变结果的操作，例如函数调用、成员访问和数组索引。

例如：

    { x: Int64 => x + 1 }(2)

是 lambda 后立即调用；而：

    store.all()[0]

包含成员访问、调用和索引组成的 postfix 链。

### 3.39 constructor、member 与 index

- constructor：构造对象的调用；
- member：对象的字段或方法；
- index：用方括号访问数组或集合元素。

中间 constructor 的类型不一定是整个 postfix 链的最终类型，所以不能在链尚可继续时过早做外层类型拒绝。

### 3.40 RSS

RSS 是 Resident Set Size，表示进程实际占用的物理内存量级。规模测试除了时间，还要观察 RSS 是否随输入异常增长。

### 3.41 PGO、LTO 与 -march=native

- PGO：Profile-Guided Optimization，先用一批输入运行程序，再让编译器针对观察到的热点优化；
- LTO：Link-Time Optimization，链接阶段跨源文件做优化；
- -march=native：让编译器针对当前宿主 CPU 使用专用指令。

公开 50 例训练出的 PGO 可能变成对公开样例的特化；-march=native 可能生成只能在 Apple 或当前 CPU 上运行的代码，所以二者禁止。通用 LTO 曾单独实测，但性能回退 2.116%，已经撤销。

### 3.42 class、interface、generic 与 arity

- class：类，声明一类对象拥有的字段和方法；
- interface：接口，规定实现者必须提供哪些能力；
- generic：泛型，让类型或函数带类型参数，例如 Box<T>；
- arity：参数数量，例如需要两个参数却只传一个，就是 arity 错误。

### 3.43 native checker

本文中的 native checker 指用 C++ 直接实现并编译成本机机器码的语义检查器，与较慢的参考 Python 路径相对。

native 不等于 Apple 专用；正式构建目标仍是通用 Linux AArch64。

### 3.44 编译开关与“正式构建完全关闭”

编译开关是在编译时决定某段诊断代码是否存在的宏。

例如 profile 或 shadow 只在专用测试构建中开启；默认正式构建时，编译器根本不把这些诊断路径放进最终二进制，而不是仅在运行时判断后跳过。这能避免诊断代码影响正式计时和协议。

---

## 4. 为什么官网 0.2 秒和本地几十毫秒不能直接比较

用户提供的官网 50 例耗时范围约为：

- 最快：223 ms；
- 最慢：310 ms；
- 中位数：约 276.5 ms。

初始本地官方镜像基线曾记录：

- SUM：1571.046 ms；
- MEDIAN：31.991 ms；
- P95：41.963 ms；
- MAX：45.946 ms。

这两组数不是同一个计时口径。

官网数字通常包含：

- 平台任务调度；
- 容器或进程管理；
- token 交互 harness；
- 日志、网络或队列；
- 固定资源检查；
- 与我们不同的服务器硬件和负载。

本地核心 A/B/A 更接近“solution 进程自身加逐 token 交互”的时间。官网每例还有很大的固定平台成本，所以即使核心程序快 10%，官网显示值也不会等比例快 10%。

因此只能这样使用数据：

- 可以比较同一轮 A 和 B 的相对变化；
- 不能拿官网 276 ms 减本地 32 ms；
- 不能拿 Apple 本机数字推算鲲鹏服务器数字；
- 不能拿不同日期的 SUM 直接相减；
- 不能承诺优化后官网会显示多少秒。

仓库里还会看到几组看似矛盾的绝对值：

| 测量来源 | SUM |
|---|---:|
| 初始独立旧基线 | 1571.046 ms |
| 第一轮累计 A/B/A 的 original control | 1913.215 ms |
| DeclarationSnapshot 同轮 control | 1646.111 ms |
| 正确性修复后 c35afae 的单独 1+9 参考 | 1181.825 ms |
| 最近三轮正式 control | 1534.476 / 1551.804 / 1579.640 ms |
| 2026-08-13 初赛→当前同轮 control / current | 1930.374 / 1531.869 ms |

这些数字不同不表示代码速度忽快忽慢，也不表示测试造假。它们来自不同日期、宿主负载、容器生命周期和构建系列。正式优化结论只看每一行对应轮次中的 control 与 candidate 相对变化；c35afae 的 1181.825 ms 只是一次新的正确性 control 量级记录，不是用来和旧版本直接比较的正式优化结果。最后一行是专门补跑的直接累计对照，因此可以得出当前相对初赛改善 20.644%。

---

## 5. 测试和接收合同

### 5.1 锁定环境

- Docker 镜像：
  docker.educg.net/compiler_system_challenge/cjchecker:20260522
- 镜像 digest：
  sha256:980dd9f2ede4f0132e9c71c71c1d6553cafd5de7cf1977c2ffe97a5ab34b8c90
- 架构：Linux AArch64
- CPU 配额：10
- 内存：8126480 KiB
- 编译器：GCC 11.4
- 官方样例仓库提交：
  88336c400e7a4a671424e3e6c46c0866c8c0af93
- 官方首错 registry SHA-256：
  2425e64184d69dd392f6cdec52dc20d42d0977cbe84be744a0ffbd1dfad374f2

### 5.2 正确性门禁顺序

每个候选必须先通过：

1. 官方 Linux AArch64 构建；
2. 官方 50 例精确首错 50/50；
3. authoritative 219/219；
4. 364 个非规模样例相对 control 的逐 token 严格差分；
5. unittest 57/57；
6. native fragment 66 × 4；
7. native context 7/7；
8. hidden fuzz 144 × 5，失败 0；
9. 官方语义语料 45/45；
10. 项目语料 57/57；
11. ASan/UBSan；
12. 9 个 scale 样例单独记录。

任何硬正确性失败都立即 REJECTED，不再测性能。

### 5.3 性能接收门槛

正确性全过后，才运行 A1 → B → A2。

ACCEPTED 至少要求：

- SUM 改善不低于 5%；
- A1/A2 漂移不超过 3%；
- MEDIAN 不显著回退；
- P95 不显著回退；
- 没有严重单例回退；
- 官方 harness 整体回退不超过 5%；
- 所有 trial 正确。

结果在 2% 到 5% 时：

- 升级为两轮完整的 1 次预热 + 21 次实测；
- 两轮都必须达到最终接受门槛才能 ACCEPTED。

小于 2% 时：

- 标记 NO PROVEN GAIN；
- 不允许与其他小改动叠加“凑 5%”。

### 5.4 反作弊和可移植性边界

明确禁止：

- 按样例名走不同代码路径；
- 硬编码公开源码片段；
- 硬编码 token 序列或首错位置；
- 判断自己是否正在 benchmark；
- 用公开 50 例训练 PGO；
- 修改官方 context、grammar、样例或计时边界；
- 延后本应立即输出的响应；
- 使用 Apple CPU 专用参数；
- 使用 -march=native。

所有正式接受优化必须对任意输入成立，并能在通用 Linux AArch64 上运行。

---

## 6. 版本演进总览

    b40791c  初赛提交，官网 AC / 50/50
       |
       +-- f85f1a3  线性 malformed 扫描         ACCEPTED
       |
       +-- 9fbc285  nominal 声明索引复用         ACCEPTED
       |
       +-- 8db0fbf  第一轮封板，累计改善 14.089%
       |
       +-- 58f03c7  generation DeclarationSnapshot ACCEPTED
       |
       +-- 0a017fd  综合语料扩展到 373，发现 217/219
       |
       +-- 6382994  修复 lambda/IIFE              218/219
       |
       +-- c35afae  修复 postfix 链               219/219
       |
       +-- 68d780d  画像扩展，正式构建关闭         当前 control
       |
       +-- 735a52e  声明起点索引，4.552%/4.777%   PROVISIONAL
       |
       +-- 75e9702  回退 735a52e
       |
       +-- 1fe3f04  测试证据和报告归档            当前 HEAD

这条时间线中，失败或未达标候选也被保留在 Git 历史里，方便审计；它们不一定存在于当前生产代码。

---

## 7. 三项正式接受的性能优化

### 7.1 线性 malformed declaration 扫描

提交：

- 代码：**f85f1a3**
- 结果记录：**1bf24ef**

#### 原来为什么慢

malformed declaration 指形状已经不可能合法的声明，例如残缺或错误的泛型/类型声明。

旧实现会在源码每次增长后，使用多个 regex 从头扫描整段源码。源码越长、token 越多，重复工作越多。

#### 做了什么

改为一次通用线性扫描：

- 从前向后看每个字节；
- 维护必要的结构状态；
- 直接识别同一组错误；
- 不减少原有报错规则。

旧 regex 保留在测试 shadow 中，对每个前缀核对新旧结果。

#### 正式结果

| 指标 | Control | Candidate | 改善 |
|---|---:|---:|---:|
| SUM | 1946.789 ms | 1842.489 ms | 5.358% |
| MEDIAN | — | — | 5.006% |
| P95 | — | — | 5.631% |
| MAX | — | — | 8.528% |

- A1/A2 漂移：0.414%
- 45 WIN / 0 LOSS
- 目标规则画像：189.903 → 2.611 ms，规则自身减少 98.625%

规则自身减少 98.625% 不等于整个程序减少 98.625%，因为程序还要启动、解析 grammar、处理语法、执行其他语义检查和协议交互。

#### 风险

风险等级：**低到中**

原因：

- 手写扫描比 regex 更容易出现边界遗漏；
- 但旧 regex shadow、ASan/UBSan、官方首错、分片和 fuzz 全过；
- 改动不影响类型规则和 model 刷新时机。

当前状态：**保留在生产代码中**

### 7.2 nominal 声明索引复用

提交：

- 代码：**9fbc285**
- 结果记录：**1de1a00**

#### nominal 是什么

nominal type 是“按名字声明的类型”，本项目主要包括 class 和 interface。

例如：

    class Box<T> { ... }
    interface Printable { ... }

语义检查需要知道：

- 类型名；
- 泛型参数；
- 父类型；
- 字段和方法；
- 当前函数属于哪个 class。

#### 原来为什么慢

一次 declared-type 检查中，多个函数会各自重新扫描整份源码，寻找同一批 class/interface 声明。

#### 做了什么

- 在同一次检查中只收集一次 nominal 声明；
- 后续作用域和泛型查询复用该集合；
- 不改变 model_dirty 或 context_dirty 时机；
- shadow 仍运行旧扫描并比较集合。

#### 正式结果

| 指标 | Control | Candidate | 改善 |
|---|---:|---:|---:|
| SUM | 1851.514 ms | 1699.616 ms | 8.204% |
| MEDIAN | — | — | 8.035% |
| P95 | — | — | 11.035% |
| MAX | — | — | 10.143% |

- A1/A2 漂移：1.433%
- 46 WIN / 0 LOSS
- declared-type 画像：335.378 → 154.839 ms，减少 53.832%

#### 风险

风险等级：**低到中**

原因：

- 复用范围只在同一次检查内；
- 没有把缓存跨越可能失效的 generation；
- 没有减少任何必要重建；
- 旧路径逐结果 shadow。

当前状态：**保留在生产代码中**

### 7.3 generation 级 DeclarationSnapshot

提交：

- 代码：**58f03c7**
- 结果记录：**3d745b6**

#### 原来为什么慢

多个消费者会重复寻找：

- 严格 class/interface 声明；
- 宽松 class 头；
- 显式返回类型函数；
- 可选返回类型函数；
- 当前函数；
- single-line 和 multiline 函数头。

这些 regex 语言相似但不完全相同，不能简单合并。

#### 做了什么

在原有 model_dirty 时机建立 DeclarationSnapshot：

- 保存捕获文本；
- 保存字节 offset 和 length；
- 保存左花括号位置；
- 保存可选的右花括号位置；
- 不保存可能因 source 扩容失效的指针或 string_view；
- 每个原始 regex family 保持独立顺序和捕获语义；
- 多个 model、context 和检查消费者共享这些记录。

#### 正式结果

| 指标 | Control | Candidate | 改善 |
|---|---:|---:|---:|
| SUM | 1646.111 ms | 1511.386 ms | 8.184% |
| MEDIAN | 34.011 ms | 30.966 ms | 8.954% |
| P95 | 42.054 ms | 38.076 ms | 9.459% |
| MAX | 46.538 ms | 41.023 ms | 11.850% |

- A1/A2 SUM 漂移：1.007%
- 46 WIN / 0 LOSS
- 没有严重单例回退
- 最大轻微正回退仅约 0.0665 ms / 0.478%
- 官方补充 harness 也快 1.837%

#### 正确性保障

调试构建保留独立旧 regex 路径，对比：

- 每个 family 的记录数量和顺序；
- 所有捕获组；
- open/close 位置；
- 完整 Model；
- 完整 FunctionContext；
- CheckStatus 的 ok 和 message。

还运行了 ASan/UBSan、官方样例、综合语料和全部前缀分片。

#### 风险

风险等级：**低到中，略高于前两项**

原因：

- 修改面更大；
- 涉及 generation 缓存和字节 offset；
- 但没有改变 dirty 时机；
- 旧 regex 全量 shadow 和 sanitizer 显著降低了风险；
- 后续两项正确性修复已叠加在它之上并重新通过 219/219。

当前状态：**保留在生产代码中**

---

## 8. 第一轮累计效果应如何解释

第一轮将原始 b40791c 与包含前两项接受优化的 8db0fbf 放在同一容器、同一轮 A/B/A 中比较：

| 指标 | Original control | Final candidate | 改善 |
|---|---:|---:|---:|
| SUM | 1913.215 ms | 1643.670 ms | 14.089% |
| MEDIAN | 39.319 ms | 34.005 ms | 13.516% |
| P95 | 50.535 ms | 42.101 ms | 16.689% |
| MAX | 55.862 ms | 46.844 ms | 16.143% |

- A1/A2 SUM 漂移：0.333%
- 46 WIN / 0 LOSS
- 50 例 × 9 次 × 3 阶段，共 1350 次正式测量，全部正确
- 官方 token_interaction_test 补充墙钟：
  - original：5763 ms；
  - final：5485 ms；
  - candidate 快 4.824%。

为什么核心 SUM 快 14.089%，官方 harness 只快 4.824%？

因为 harness 还包含：

- Python 驱动；
- 进程启动和等待；
- token 编码；
- 管道通信；
- 固定环境管理。

这些外部成本没有被 C++ 语义扫描优化掉，所以整体收益会被稀释。

### 8.1 初赛到当前版本的直接累计复测

在三项优化和两项正确性修复全部完成后，额外使用初赛 `b40791c` 作为 A、当前 accepted control `68d780d` 作为 B，执行了新的同容器 A/B/A。

| 指标 | 初赛 A1/A2 合成 control | 当前版本 | 改善 |
|---|---:|---:|---:|
| SUM | 1930.374 ms | 1531.869 ms | 20.644% |
| MEDIAN | 39.783 ms | 31.465 ms | 20.908% |
| P95 | 50.844 ms | 38.847 ms | 23.595% |
| MAX | 55.994 ms | 42.011 ms | 24.973% |
| 首响应 SUM | 434.243 ms | 401.002 ms | 7.655% |
| 首错检测 SUM | 1813.375 ms | 1417.541 ms | 21.829% |

- A1/A2 SUM 漂移：1.147%
- A1/A2 MEDIAN 漂移：1.298%
- 逐例相对漂移中位数：1.156%
- 47 WIN / 0 LOSS
- 无严重单例回退
- 实际上 50 个样例全部变快；其中 3 个改善没有超过显著性噪声阈值
- 三个阶段全部 50/50、failed trial 为 0

该 20.644% 是目前唯一可以直接回答“当前版本相对初赛版本快多少”的同轮累计结果。它仍然是官网样例在本地官方镜像的复现结果，不是已关闭官网服务器重新给出的耗时。

---

## 9. 全部未接受候选及教训

### 9.1 汇总表

| 候选 | 正式结果 | 判定 | 是否仍在生产代码 |
|---|---:|---|---|
| accepted token 容器改为计数器 | SUM 回退 0.458% | NO PROVEN GAIN | 否，已回退 |
| FunctionContext 去深拷贝 | SUM 回退 0.357% | NO PROVEN GAIN | 否，已回退 |
| 类型规范化复用 | SUM 改善 0.320% | NO PROVEN GAIN | 否，已回退 |
| TokenTable 单缓冲 | SUM 改善 0.331% | NO PROVEN GAIN | 否，已回退 |
| 通用 -flto | SUM 回退 2.116% | REJECTED | 否，已回退 |
| 每 token 直接 write | SUM 改善 0.057% | NO PROVEN GAIN | 否，已回退 |
| 增量 lexer/source facts | SUM 回退 0.293% | NO PROVEN GAIN | 否，已回退 |
| commit-gated model rebuild | 官方首错 32 → 33 | REJECTED：正确性 | 否，已回退 |
| stable-event model rebuild | 官方首错 32 → 33 | REJECTED：正确性 | 否，已回退 |
| nominal keyword guard | 21 次双轮改善 2.179% / 2.723% | PROVISIONAL 未升格 | 否，已回退 |
| TokenTable/grammar 启动并行 | 21 次双轮改善 4.079% / 4.062% | PROVISIONAL 未升格 | 否，已回退 |
| 每 token syntax/semantic 并行 | SUM 回退 11.036% | REJECTED | 否，已回退 |
| DeclarationSnapshot 起点索引 | 21 次双轮改善 4.552% / 4.777% | PROVISIONAL 未升格 | 否，已回退 |

### 9.2 为什么“看起来减少复制”却可能更慢

CPU 现代优化和内存布局很复杂：

- 原复制可能非常小，已经被编译器优化；
- 新的引用或分支可能破坏局部性；
- 代码体积增大可能影响指令缓存；
- 构造新索引本身也有成本；
- 整个进程还有大量固定开销。

所以“理论工作量减少”不等于“墙钟一定减少”。这也是必须实测 A/B/A 的原因。

### 9.3 为什么 -flto 没有保留

LTO 是 Link Time Optimization，即链接时优化。它让编译器跨多个目标文件做优化。

它是通用编译技术，但不保证一定更快：

- 可能改变函数内联和代码布局；
- 代码体积可能增大；
- 当前热点不一定能靠跨文件优化消除。

实测 SUM 回退 2.116%，所以按合同拒绝。

### 9.4 为什么直接 write 没有用

把 C++ iostream 输出换成更底层的 POSIX write，理论上可能减少封装开销。

但本题每个 token 只输出极少字节，而且必须立即 flush。真正成本主要不在这层调用，实测只改善 0.057%，无法证明有效。

### 9.5 为什么并行化没有用

#### 启动并行

TokenTable 和 grammar 初始化可以理论重叠。正确性通过，但两轮 21 次只改善 4.079% 和 4.062%，未达到预先规定的 5%。

并发还带来：

- 线程创建失败处理；
- 异常优先级；
- 线程安全；
- 单核退化；
- 调度噪声。

收益不足以抵消长期维护复杂度，所以回退。

#### 每 token 并行

语法和语义可以在两个线程同时算，但每个 token 都需要：

- 加锁；
- 条件变量唤醒；
- 上下文切换；
- 等待两边完成。

单个 token 的工作太小，同步开销超过了可重叠计算，最终慢 11.036%，50 个样例全部变慢。

### 9.6 为什么 dirty-generation 是高风险方向

它想通过减少 model 重建次数来提速，但两种不同实现都让：

    err_interface_sig_mismatch

的首错从 token 32 移到 token 33。

这说明某个前缀仍需要及时刷新 model。即使最终完整程序仍会拒绝，精确首错已经错误。

这是最有价值的一次失败教训：不能为了性能改变语义事实何时生效。

---

## 10. 新综合语料与两项正确性修复

### 10.1 373 例是怎样组成的

| 类型 | 数量 | 含义 |
|---|---:|---|
| 完整合法程序 | 214 | 所有 token 输入后应保持可接受 |
| 已提交错误程序 | 120 | 到目标错误后必须拒绝 |
| 可继续补全前缀 | 39 | 当前不完整，但仍可能补成合法程序 |
| 合计 | 373 | 覆盖 30 个测试族 |

按证据强度再分：

| 证据层级 | 数量 | 如何使用 |
|---|---:|---|
| authoritative | 219 | 硬门禁，必须 219/219 |
| diagnostic_spec_pending | 145 | 只报告差异，不能强行改代码 |
| diagnostic_scale | 9 | 记录耗时、超时和资源增长 |

### 10.2 为什么不是要求 373/373

测试数量多不等于每个标签都拥有官方证明。

145 个 spec-pending 包含当前 grammar 的扩展语法或尚无独立规范依据的设计。若为了把数字凑成 373/373 而修改实现，可能反而偏离赛事真实规则。

所以正确说法是：

> authoritative 219/219；40 个 spec-pending 标签差异待规范确认；两个 scale 超时待性能专项处理。

不能说：

> 已经证明完整仓颉语言 100% 正确。

### 10.3 Lambda/IIFE bug

示意代码：

    let result: Int64 = { x: Int64 => { x + 1 } }(2)

在收到内层 block 的右花括号时，外层 lambda 和调用后缀还可能继续。旧代码把这个右花括号当成语义提交点，过早拿函数类型与 Int64 比较。

修复提交 **6382994**：

- 只有顶层分号、顶层换行或真正函数体结束才提交；
- 表达式内部右花括号不提前提交外层声明；
- 内部已经确定的 lambda 参数和返回错误仍立即报告。

修复后 authoritative：217/219 → 218/219。

风险等级：**低到中**

这是有意改变错误时机的正确性修复，不是纯等价优化；但有独立 oracle、正反成对测试和全分片验证。

### 10.4 Call/member/index bug

示意代码：

    let result: Int64 = Store([[1]]).all()[0][0]

中间的：

    Store([[1]])
    Store([[1]]).all()
    Store([[1]]).all()[0]

都可能继续追加后缀，最终类型可能继续变化。旧代码可能在中间前缀就做外层类型不匹配。

修复提交 **c35afae**：

- call、array、member、index 的合法中间结果可标记为“后缀仍可能改变类型”；
- 只延迟外层 annotated initializer mismatch；
- 参数数量、参数类型、未知成员、错误索引等内部确定错误不延迟。

修复后 authoritative：218/219 → 219/219。

风险等级：**低到中**

### 10.5 新 control 的完整门禁

- unittest：57/57
- native fragment：66 × 4
- native context：7/7
- hidden fuzz：144 × 5，失败 0
- 官方公开：50/50 精确首错
- 官方语义：45/45
- 项目语料：57/57
- non-scale comprehensive：364/364
- authoritative：219/219
- ASan/UBSan：全过

当前没有已知 authoritative 正确性失败。

这仍不等于“隐藏测试必然全过”，因为我们不知道关闭后的官方隐藏输入，也没有完整仓颉规范 oracle。

如果队员在根 README 中看到 217/219，那是“373 例刚接入时”的历史状态，不是最终状态。随后 6382994 和 c35afae 已把它修复到 219/219；最终结论应以本文和正确率优先后续报告为准。

---

## 11. 最近一次未升格的声明扫描候选

候选：**735a52e**

回退：**75e9702**

### 11.1 做了什么

先用通用字节扫描找出所有可能以 class、interface、func、main 开始的位置，再从这些可能位置运行原来的 8 族 regex。

它没有：

- 识别样例名；
- 识别公开源码；
- 跳过注释或字符串；
- 更改 regex 捕获语义；
- 更改 dirty 时机。

### 11.2 正确性结果

- 官方 50/50；
- authoritative 219/219；
- 非规模 364/364；
- 相对 control：
  - 728 次双协议；
  - 238 次安全前缀；
  - 26 次输入边界；
  transcript、首错、退出码、stderr 全等；
- shadow、ASan/UBSan 全过。

所以它不是因为正确性风险被回退，而是因为性能没有达到事前门槛。

### 11.3 性能结果

| 轮次 | Control SUM | Candidate SUM | 改善 | 漂移 | WIN/LOSS |
|---|---:|---:|---:|---:|---:|
| 1 + 9 | 1534.476 ms | 1469.590 ms | 4.229% | 0.478% | 32/0 |
| 1 + 21 R1 | 1551.804 ms | 1481.171 ms | 4.552% | 1.858% | 36/0 |
| 1 + 21 R2 | 1579.640 ms | 1504.180 ms | 4.777% | 0.427% | 36/0 |

两轮扩展仍低于 5%，最终状态：

**PROVISIONAL（扩展后未升格），已回退**

这不是 REJECTED，也不是 NO PROVEN GAIN。它确实稳定变快，只是未达到团队事先锁定的正式接收标准。不能在看到 4.777% 后临时降低门槛，否则测试合同失去意义。

---

## 12. 当前准确性到底有多可信

### 12.1 可以有把握说的

当前版本已经证明：

- 在锁定官方镜像中可以构建和运行；
- 公开 50 例首错完全一致；
- 219 个独立证据非规模样例全部通过；
- 45 个官方语义样例全部通过；
- 57 个项目样例全部通过；
- 固定 fuzz 和多种输入分片没有发现差异；
- 364 个非规模样例可用于性能候选的严格 control 差分；
- 没有发现 ASan/UBSan 内存或未定义行为问题；
- 没有按公开样例做特化。

### 12.2 不能保证的

当前证据不能证明：

- 官方所有隐藏样例一定通过；
- 完整仓颉语言每个角落都实现正确；
- 145 个 spec-pending 的标签都正确；
- 40 个诊断差异一定是代码 bug；
- 两个超长规模输入已经解决；
- 优化后官网总分仍然或必然是多少；
- Apple 本机耗时等于官方鲲鹏服务器耗时。

### 12.3 推荐对外表述

推荐说：

> 当前版本通过官方公开 50 例精确首错、45 例官方语义语料、57 例项目语料、219 个独立 oracle-backed 非规模综合样例，以及固定 fuzz、分片差分、context 差分和 ASan/UBSan。当前没有已知 authoritative 正确性失败；仍保留 40 个规范待确认诊断差异和两个规模超时。

不推荐说：

> 当前版本已经 100% 覆盖所有隐藏测试和完整仓颉规范。

---

## 13. 各历史版本风险比较

| 版本 | 官方 50 | authoritative | 主要情况 | 综合风险 |
|---|---:|---:|---|---|
| b40791c | 50/50 | 后来发现 217/219 | 初赛 AC，但存在两个已确认 false reject | 中 |
| 8db0fbf | 50/50 | 后来发现 217/219 | 前两项优化已接受，正确性修复尚未加入 | 中 |
| 58f03c7 | 50/50 | 217/219 | 三项性能优化已接受，但仍有两个 false reject | 中 |
| 6382994 | 50/50 | 218/219 | 只修复 lambda，postfix 问题仍在 | 中低 |
| c35afae | 50/50 | 219/219 | 最精简的完整正确性生产锚点 | **最低** |
| 68d780d | 50/50 | 219/219 | 在 c35 上加默认关闭画像 | **很低** |
| 735a52e | 50/50 | 219/219 | 正确性全过，但收益不足 5% | 语义低中；维护成本不值得 |
| 当前 HEAD 1fe3f04 | 50/50 | 219/219 | 生产文件与 68d 相同，只多审计资料 | 很低 |

这里的“最低”是相对当前已测试版本，不表示绝对零风险。

### 为什么 c35afae 风险最低

- 包含三个已正式接受的通用性能优化；
- 包含两个 oracle 支持的正确性修复；
- 不包含后来未升格的声明起点索引；
- 不包含额外画像脚手架；
- 已完成完整 AArch64、fuzz、差分和 sanitizer 门禁。

### 为什么继续开发用 68d780d

68d780d 的画像代码只在诊断编译开关开启时编译。正式构建中这些路径完全关闭，因此运行行为与 c35afae 一致。

继续优化需要画像定位热点，所以 68d780d 更适合作为研发 control。

---

## 14. 剩余优化空间和风险

### 14.1 grammar 不动时

已识别的主要扫描耗时毛上限：

- DeclarationSnapshot 构建：144.903 ms；
- range regex：46.610 ms；
- if-branch regex：57.115 ms；
- 完全免费消除的算术合计：248.628 ms。

相对当前同轮 SUM，这只是约 15.7% 到 16.2% 的理论毛上限，而且新索引本身也要耗时。

现实判断：

- 剩余低到中风险空间约 **5% 到 10%**；
- 再拿到稳定 5% 已经比前期困难；
- 不应该为了凑 5% 把多个独立失败微优化绑在一起。

### 14.2 进一步声明扫描

最近候选已经稳定做到 4.552% / 4.777%，说明方向有效，但距离 5% 仍差一点。

风险：

- 更激进的合并可能改变 8 族 regex 的细微语言；
- 注释、字符串和畸形声明中的旧行为也必须保持；
- offset、optional close 和匹配顺序不能漂移。

只有能保持逐前缀旧 regex shadow 时才值得继续。

风险等级：**中**

### 14.3 range 与 if-branch 统一结构索引

两项 regex 的完全消除上限约 103.725 ms，只占 control 的约 6.6% 到 6.8%，还没有扣除新索引成本，低于预先规定的 7% 立项门槛。

风险：

- 可能改变检查调用时机；
- 可能改变 ExpressionTyper 的输入；
- 两个独立不足 5% 的改动不能为了凑数强行绑在一起。

风险等级：**中到高**

当前状态：**未实施**

### 14.4 grammar 改写

官方 50 画像中：

- syntax 约 319.428 ms；
- grammar 初始化约 107.759 ms。

理论上：

- syntax 全部归零的硬上限约占总 SUM 20%；
- 连 grammar 初始化也凭空归零，硬上限约 27% 到 28%。

这些是不可实现的数学天花板，不是预期收益。现实官方 50 收益只能谨慎看 **0% 到 10%**。

但规模输入上存在巨大潜力：

#### 300 locals

| locals | syntax | semantic | 总耗时 |
|---:|---:|---:|---:|
| 100 | 0.878 s | 0.066 s | 0.962 s |
| 150 | 3.071 s | 0.143 s | 3.237 s |
| 200 | 7.926 s | 0.252 s | 8.206 s |
| 250 | 15.470 s | 0.377 s | 15.873 s |
| 300 | 28.054 s | 0.557 s | 28.652 s |

syntax 经验增长接近 O(n³)，300 locals 中占约 98%。

仅在临时目录的机制实验中，把右递归：

    statements ::= statement (ws statements)?

改成迭代形式：

    statements ::= statement (ws statement)*

300 locals：

- syntax：28.054 s → 0.191 s；
- 完整：28.652 s → 0.752 s。

这是约 38 倍完整程序加速，但它只证明性能根因，不证明所有增量前缀语言等价。

#### 4KB identifier

- 4135 字节；
- 527 个 cl100k token；
- native semantic 处理所有前缀约 12.8 ms；
- 完整 solution 45 秒只回复约 101/527；
- syntax 随长度接近立方增长。

根因同样是 grammar 中：

- 可空 whitespace；
- 可选分号；
- var/assign/expression 共享标识符前缀；
- 增量 matcher 保留大量歧义状态。

grammar 改写风险等级：**最高**

它可能改变：

- 哪些不完整前缀被接受；
- 首错 token；
- 注释和换行边界；
- 可选分号行为；
- lambda/block 嵌套；
- 所有官方样例。

如果未来立项，必须新建独立高风险分支，让旧 matcher 和新 matcher 对每个前缀同时运行并比较，绝不能直接把临时实验规则放进生产。

### 14.5 快速进程退出

表面上“检测到首错到进程结束”约有 2 ms/例，但其中大部分来自 Python wait 和系统调度。

进程内对象析构画像总计只有：

    16.552 ms / 50 例

约占 SUM 1.1%，低于 6% 实施门槛。

std::_Exit 还会跳过：

- profiler 输出；
- LSan 退出检查；
- 静态对象析构；
- atexit 回调。

因此没有实施。

风险等级：**收益不足，不建议做**

### 14.6 不现实的总上限

如果假设启动、syntax、semantic 和析构全部变成 0，数学上可能消除约 67% 到 69% 的正式 SUM。

这只是算术天花板，因为：

- token 管道通信不能为 0；
- 进程启动不能为 0；
- Python harness 不能为 0；
- 正确语法和语义计算不能为 0；
- 每 token 必须立即输出。

更可信的后续判断：

- 保持 grammar 不动：新增 **5% 到 10%** 已属不错；
- 承担 grammar 高风险并证明逐前缀等价：官方 50 可能再有 **0% 到 10%**，scale 可能大幅改善；
- 不应把 67% 到 69% 当项目目标或承诺。

---

## 15. 继续优化的风险是否值得

### 15.1 当前阶段与早期不同

早期热点是“相同整段源码被明显重复扫描”，容易找到可证明等价的收益。

现在剩余热点更多涉及：

- 缓存何时失效；
- 前缀何时正式提交；
- grammar 的增量状态；
- 多个检查的顺序；
- 线程同步；
- 隐藏语言语义。

这些改动更容易影响首错和隐藏正确率。

### 15.2 风险排序

| 方向 | 潜在收益 | 正确性风险 | 工程复杂度 | 建议 |
|---|---:|---:|---:|---|
| 保持旧 regex shadow 的局部扫描优化 | 约 2%–5% | 低中 | 中 | 可谨慎尝试 |
| 更完整的声明索引 | 约 5% 左右 | 中 | 高 | 只有画像上限足够时做 |
| range/branch 结构索引 | 小于理论 6.8% | 中高 | 高 | 当前停止 |
| 启动并行 | 已测约 4.1% | 中 | 高 | 不重试 |
| 每 token 并行 | 已测慢 11.0% | 中高 | 高 | 不重试 |
| dirty-generation | 理论可能较高 | **高，已出现首错漂移** | 高 | 不重试粗粒度方案 |
| grammar 重写 | scale 极高；官方未知 | **最高** | 很高 | 必须独立高风险计划 |

### 15.3 什么时候应该停止

满足任一条件时建议停止当前优化轮：

- 没有理论可消除成本达到 5% 到 7% 的独立通用候选；
- 必须改变 dirty 刷新时机才能继续；
- 必须修改 grammar 才能继续；
- 只能把多个小于 2% 或未升格候选叠加凑数；
- shadow 无法覆盖所有改变过的可观察行为；
- 新增维护复杂度明显超过可能收益。

当前最稳妥的选择是保留 c35afae/68d780d，除非团队愿意把 grammar 规模问题作为独立高风险研究项目。

---

## 16. 给队员的版本选择建议

### 场景 A：今天就要交一份最保守代码

推荐：

1. 从 **c35afae** 新建干净 submission 分支；
2. 重新默认构建，不打开 profile/shadow 宏；
3. 跑官方 Linux AArch64 全门禁；
4. 检查 context、grammar、TokenTable 资源 hash；
5. 打包；
6. 不带 benchmark 结果、临时二进制和诊断日志。

理由：源码面最精简，同时拥有 219/219 和全部已接受性能优化。

### 场景 B：团队还要继续研究性能

推荐：

1. 以 **68d780d** 为 control；
2. 每个候选新建独立本地提交；
3. 先跑正确性，再跑性能；
4. 失败立即 revert；
5. 只有 ACCEPTED 候选才能成为下一 control；
6. 不推送远程前先做一次提交范围审计。

### 场景 C：只想复现初赛

使用 **b40791c** 可以复现初赛公开 50/50 和当时 AC 状态，但它存在后来确认的两个 false reject，所以不建议作为新的最终提交。

### 场景 D：想保留最完整审计历史

保留当前 HEAD **1fe3f04**。它的生产文件与 68d780d 相同，并包含：

- 失败候选历史；
- 回退记录；
- 原始性能 trial；
- 测试合同；
- 正确性报告；
- 画像和 scale 诊断。

审计仓库与最终提交包可以分开：一个用于证明过程，一个用于最小化提交风险。

---

## 17. 队内同步时最容易说错的五件事

### 错误说法 1

“我们从 1571 ms 优化到 1511 ms，所以只快了 3.8%。”

为什么错：

1571 和 1511 来自不同轮次、不同环境，不能直接相减。

正确说法：

- 第一轮同轮累计改善 14.089%；
- DeclarationSnapshot 在自己的同轮 A/B/A 改善 8.184%。

### 错误说法 2

“三项优化一共正式快了 22.273%。”

为什么错：

14.089% 和 8.184% 不能直接相加。

正确说法：

三项分别通过正式验收；之后补跑的初赛到当前直接同轮 A/B/A 给出的正式累计改善是 20.644%。

### 错误说法 3

“现在 373/373 全正确。”

为什么错：

只有 219 个 authoritative 是硬证据；145 个 spec-pending 中仍有 40 个标签差异，9 个 scale 中有 2 个超时。

正确说法：

authoritative 219/219，非规模基础设施 364/364，诊断差异和 scale 超时已如实保留。

### 错误说法 4

“735a52e 有 bug，所以被回退。”

为什么错：

它的完整正确性、shadow 和 sanitizer 都通过。回退原因是 4.552% / 4.777% 未达到预定 5% 性能门槛。

正确说法：

它是 PROVISIONAL 未升格，不是正确性 REJECTED。

### 错误说法 5

“临时 grammar 实验快 38 倍，所以正式版也能快 38 倍。”

为什么错：

38 倍只发生在 300 locals 病态 scale 样例，还没有证明新旧 grammar 对所有增量前缀等价。官方 50 的现实收益可能远小于这个数字。

正确说法：

实验已经定位 scale 根因，但 grammar 生产改写尚未实施，必须另立高风险计划。

---

## 18. 建议的下一步

### 18.1 如果正确率优先级不变

建议先停止低收益微优化，冻结：

- 生产锚点：c35afae；
- 研发 control：68d780d；
- 当前审计 HEAD：1fe3f04。

然后由队员共同决定是否接受 grammar 研究的风险。

### 18.2 如果决定研究 grammar

先做诊断候选，不立即替换生产 grammar：

1. 同时构造旧 matcher 和新 matcher；
2. 对每个 token 前缀比较布尔结果；
3. 覆盖官方 50、官方语义 45、项目 57、fuzz 144、非规模 364；
4. 覆盖 byte/random/line/cl100k/whole；
5. 单独测 locals 0–300、identifier 128–4096；
6. 记录 grammar compile、matcher、每 token syntax、峰值 RSS；
7. 任一前缀差异立即停止；
8. 全正确后才进入正式 A/B/A。

### 18.3 如果决定不冒 grammar 风险

当前版本已经是合理的正确率优先封板点：

- 三项独立性能优化已正式接受；
- 两个新发现的 authoritative bug 已修复；
- 219/219；
- 50/50 精确首错；
- 完整 sanitizer 和差分证据；
- 未保留未达标候选；
- 未做样例特化或平台特化。

---

## 19. 可直接发给队友的简短摘要

> 初赛代码 b40791c 在官网 AC、25 分、公开 50/50。之后我们建立了严格的 Linux AArch64 A/B/A 和正确性门禁，正式保留了三项通用优化：线性 malformed 扫描、nominal 声明索引复用、generation 级 DeclarationSnapshot。各项独立验收结果不能直接相加；最终补跑的初赛→当前同轮 A/B/A 为 1930.374→1531.869 ms，正式累计改善 20.644%，且 50/50、47 WIN/0 LOSS。
>
> 扩展到 373 例综合语料后，发现两个公开 50 例没覆盖的通用前缀误拒：lambda/IIFE 与 call/member/index 链。两个独立提交修复后，官方首错仍为 50/50，authoritative 恢复 219/219，并通过 45 官方语义、57 项目、144×5 fuzz、66×4 fragment、context 和 ASan/UBSan。
>
> 当前最精简、风险最低的生产锚点是 c35afae；继续研发用 68d780d，当前 HEAD 的生产逻辑与它相同。最近的声明起点索引候选虽然正确性全过且快 4.552%/4.777%，但没有达到事先约定的 5%，已回退。
>
> 目前仍有 40 个 spec-pending 标签差异和两个 scale 超时，不能宣称 373/373 或隐藏测试 100%。两个超时已定位到 grammar/XGrammar 的近立方状态膨胀。临时 grammar 实验能让 300 locals 从 28.652 秒降到 0.752 秒，但尚未证明所有增量前缀等价，因此没有进入生产。后续若继续，应把 grammar 改写作为独立高风险项目；如果继续坚持正确率优先，当前版本已经适合封板。

---

## 20. 证据文件索引

- [优化测试合同](OPTIMIZATION_TESTING_CONTRACT.md)
- [第一轮累计优化封板报告](baseline_results/20260812_final_optimization_report.md)
- [线性 malformed 扫描报告](baseline_results/20260812_linear_malformed_verdict.md)
- [nominal 声明索引报告](baseline_results/20260812_nominal_index_verdict.md)
- [DeclarationSnapshot 正式接受报告](baseline_results/20260812_2257_declaration_snapshot_verdict.md)
- [373 例综合语料接入报告](baseline_results/20260813_comprehensive_373_integration_verdict.md)
- [正确率优先后续报告](baseline_results/20260813_correctness_first_followup_report.md)
- [声明起点索引候选报告](baseline_results/20260813_snapshot_candidate_index_verdict.md)
- [性能结果目录说明](baseline_results/README.md)
- [综合语料说明](test_cases/comprehensive/README.md)

---

## 21. 最终结论

从初赛代码到当前版本，最重要的成果不是单一的百分比，而是形成了可审计的优化方法：

1. 先锁定官方行为；
2. 用全语料画像找热点；
3. 每个候选独立提交；
4. 用 shadow 和 differential 证明通用等价；
5. 用 sanitizer 排除内存风险；
6. 正确性全过后才测 A/B/A；
7. 达不到事先门槛就回退；
8. 不为样例、Apple 环境或 benchmark 做特化；
9. 新语料发现真实正确性问题时，先修正确性再建立新 control；
10. 对不确定规范和规模超时如实保留，不用改标签制造“全绿”。

当前建议：

- **最保守最终生产锚点：c35afae**
- **继续研发性能 control：68d780d**
- **当前审计历史：1fe3f04**
- **未推送远程**

如果团队不准备承担 grammar 改写的高风险，当前版本已经是一个正确率、速度、可解释性和维护成本之间较平衡的封板点。
