# 2026全国大学生计算机系统能力大赛编译系统设计赛-仓颉代码片段语义检查 文档核心总结
本文档是**华为毕昇杯·仓颉代码片段语义检查**赛题的官方技术方案，明确了比赛目标、初赛/决赛规则、评分标准、仓颉语法子集、参考文献及提交规范，核心是实现仓颉代码片段的**增量式语法+语义检查**。

## 一、初赛仓颉语言测试用例分布
1. **按语法特性分层占比**
    - 70%：无lambda表达式，泛型**显式标注类型参数**
    - 15%：含lambda表达式，泛型**显式标注类型参数**
    - 15%：含lambda表达式，泛型**可省略类型参数（需类型推导）**
2. **数据集构成**：50个公开测试用例 + 50个非公开测试用例

## 二、初赛评分标准
初赛总分=**功能测试分+性能测试分**，单测试用例满分10分，最终成绩为所有用例得分累加。
1. **功能测试（单用例5分）**
    - 判定标准：输出完全符合预期、单用例执行时间≤5秒
    - 扣分规则：输出错误/执行超时，单用例功能分直接计0
2. **性能测试（单用例最高5分）**
    - 参与资格：仅功能测试正确的用例可参与
    - 排序计分：第1名5分、第2名4分、第3名3分、第4名2分、第5名1分，第6名及以后不计分

## 三、仓颉子集语法描述（附录一核心）
赛题限定仓颉语言子集，覆盖**类型、表达式、声明、类/接口、控制流、Lambda**六大核心模块：
1. **基础类型**：Int8/Int16/Int32/Int64、Float32/Float64、Bool、Rune、String
2. **类型体系**：基础类型、数组`Array<T>`、泛型类型/类型参数、自定义标识符类型
3. **表达式**：字面量、算术/逻辑/比较运算、函数/方法调用、索引/属性访问、块表达式、Lambda表达式、break/continue/return
4. **声明语句**：变量（var/let）、函数、类、接口
5. **类与接口**：支持泛型、继承/实现关系，包含字段、方法、构造器、抽象方法
6. **控制结构**：if分支、while循环、for-in/range循环
7. **Lambda**：支持可选类型标注的参数，`=>`连接表达式/代码块

以下为官方给出的具体语法描述：

---

(* Primitive Types and Values *)
primitive_type = "Int8" | "Int16" | "Int32" | "Int64" | "Float32" | "Float64" | "Bool" | "Rune" ;
type = primitive_type | "String" | "Array" "<" type ">" | identifier [ type_arguments ] | type_variable ;
type_variable = identifier ;
(* Type Parameters *)
type_parameter = identifier ;
type_arguments = "<" type { "," type } ">" ;
(* Expressions *)
expression = literal
| identifier
| expression ( "+" | "-" | "*" | "/" | "%" ) expression
| expression ( "==" | "!=" | "<" | ">" | "<=" | ">=" ) expression
| expression ( "&&" | "||" ) expression
| "!" expression
| expression "." identifier [ "(" [ argument_list ] ")" ]
| identifier [ "(" [ argument_list ] ")" ]
| "(" expression ")"
| "[" [ expression { "," expression } ] "]"
| expression "[" expression "]"
| block_expression
| if_expression
| while_expression
| for_expression
| lambda_expression
| "break"
| "continue"
| "return" [ expression ] ;
literal = number | string | "true" | "false" ;
argument_list = argument { "," argument } ;
argument = expression | identifier ":" expression ;
(* Block Expressions *)
block_expression = "{" ( variable_declaration | expression )* "}" ;
(* Lambda Expressions *)
lambda_expression = "{" [ lambda_parameters ] "=>" ( expression | block_expression ) "}" ;
lambda_parameters = lambda_param { "," lambda_param } ;
lambda_param = identifier [ ":" type ] ; (* types are optional in lambdas *)
(* Declarations *)
declaration = variable_declaration
| function_definition
| class_definition
| interface_declaration ;
variable_declaration = ( "var" | "let" ) identifier "=" expression ;
(* Function Definitions *)
function_definition = [ "public" | "private" ] [ "static" ] "func" identifier "(" [ parameter_list ] ")" ":" type
block_expression ;
parameter_list = parameter { "," parameter } ;
parameter = identifier [ "!" ] ":" type [ "=" expression ] ;
(* Class Definitions *)
class_definition = [ "public" ] "class" identifier [ type_parameters ]
[ "<:" type [ "&" type_list ] ]
class_body ;
type_list = type { "&" type } ;
class_body = "{" { class_member } "}" ;
class_member = field_definition | method_definition | constructor_definition ;
(* Interface Declaration *)
interface_declaration = [ "public" ] "interface" identifier [ type_parameters ]
[ "<:" type_list ] interface_body ;
interface_body = "{" { abstract_method } "}" ;
(* Fields and Methods *)
field_definition = [ "public" | "private" ] [ "static" ]
type identifier [ "=" expression ] ;
method_definition = [ "public" | "private" ] [ "static" ]
[ type_parameters ] "func" identifier "(" [ parameter_list ] ")" ":" type block_expression ;
abstract_method = [ type_parameters ] "func" identifier "(" [ parameter_list ] ")" ":" type ;
constructor_definition = [ "public" | "private" ] "init"
"(" [ parameter_list ] ")" block_expression ;
(* Control Structures *)
if_expression = "if" "(" expression ")" block_expression
[ "else" ( block_expression | if_expression ) ] ;
(* Loop Expressions *)
while_expression = "while" "(" expression ")" block_expression ;
for_expression = "for" "(" identifier "in" expression ")" block_expression
| "for" "(" identifier "in" range_expression ")" block_expression ;
range_expression = expression ".." [ "=" ] expression [ ":" expression ] ;

---

## 四、参考文献
[1] Terry Koo, Frederick Liu, Luheng He. Automata-based constraints for language model decoding.
[2] Luca Beurer-Kellner, Marc Fischer, Martin Vechev. Guiding LLMs The Right Way: Fast, Non-Invasive Constrained Generation. ICML 2024.
[3] Niels Mündler, Jingxuan He, Hao Wang, Koushik Sen, Dawn Song, Martin T. Vechev. Type-Constrained Code Generation with Language Models. Proc. ACM Program. Lang. 9(PLDI), 2025.

## 五、比赛核心执行规则
1. **检查模式**：**增量式**（逐token输入，每读入1个token立即输出1/0；输出0则提前终止），非全量检查
2. **代码查重要求**
    - 学术诚信：禁止代码抄袭、未标注使用第三方代码
    - 查重标准：代码重复率**>20%**，直接取消参赛资格
    - 第三方代码：需在**设计文档+源码头部**明确标注，遵守开源协议
3. **核心任务**：检查代码片段能否续写为合法仓颉代码，校验**语法+类型/作用域语义**
4. **环境支持**：赛方提供import导入的全局变量/函数/类/接口定义，无需处理包依赖
5. **运行环境**：鲲鹏920 ARM服务器 + openEuler操作系统
6. **决赛规则**：分作品优化+现场答辩；成绩=客观分(30%)+方案文档(20%)+答辩(50%)

## 六、可实现计划与ToDoList
### （一）分阶段实现计划（总周期10-12天）
1. **准备阶段（1-2天）**：梳理赛题规则、研读仓颉语法子集、搭建开发环境
2. **核心开发阶段（5-7天）**：实现增量式词法/语法/语义检查，优先完成70%基础用例
3. **测试优化阶段（2-3天）**：用公开用例调试，优化执行速度，补充lambda/泛型推导
4. **提交阶段（1天）**：完成文档、视频，合规检查后提交

### （二）落地ToDoList
#### 1. 前期准备
- [ ] 通读赛题文档，标记核心规则与约束
- [ ] 搭建鲲鹏ARM+openEuler模拟开发环境
- [ ] 下载仓颉1.0.0官方文档、公开测试用例与评测工具
- [ ] 学习参考文献中**增量式自动机检查**核心思路

#### 2. 功能开发（优先级从高到低）
- [ ] 实现**逐token增量输入**的程序框架
- [ ] 完成基础语法检查（无lambda、显式泛型，覆盖70%用例）
- [ ] 实现类型检查、作用域检查（语义核心）
- [ ] 补充lambda表达式语法/语义检查（15%用例）
- [ ] 实现泛型类型参数推导（最后15%用例，可选）
- [ ] 接入赛方提供的全局符号表，无需处理import/包依赖

#### 3. 性能优化
- [ ] 预计算类型可达性，减少重复检查
- [ ] 优化单token处理逻辑，确保单用例执行<5秒
- [ ] 压测优化，提升执行速度排名

#### 4. 测试与调试
- [ ] 用50个公开用例全量测试，修正输出错误
- [ ] 模拟超时场景，优化耗时瓶颈
- [ ] 验证增量输出逻辑：正确位置首次出0，其余输出1

#### 5. 提交与合规
- [ ] 编写PDF设计文档（功能/性能/创新/问题解决）
- [ ] 录制≤5分钟MP4作品介绍视频
- [ ] 源码+文档标注第三方引用，检查代码重复率<20%
- [ ] 在竞赛平台提交源码、文档、视频

