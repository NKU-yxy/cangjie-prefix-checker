# v8 本地验证记录（2026-08-18 16:14 打包，未上传）

## 交付物

- `cangjie-checker_v8_20260818.zip`，891,032 B < 1MB，sha256：
  `a4e9f06cf10d198df9fb2c3ce60feaac7e325a4f59657e93eed0840978d998c5`
- 方案：v7（9954edd）基线 + 差分 fuzzer 驱动的 4 项定点修复，仅改
  `cpp/native_semantic.cpp`（+50/-6）。文件集与 v7 zip 逐项一致（109 文件），
  仅 native_semantic.cpp 内容不同（zip 内 sha256 == 宿主工作区源码）。

## 本轮改动（均来自 statement 级差分 fuzzer 实测分歧）

| 修复 | 位置 | 行为变化 | 依据 |
|---|---|---|---|
| A. mixed/logical 延迟 | `should_defer_expression_error` | 语句未提交（无 `;`/`}`）时，`mixed numeric arithmetic`、`logical operands require Bool` 不再即时报，改到下一语句首 token（`}`/下条语句） | 官方锚点：err_arith_mixed_family / err_logical_non_bool 在 println 处（下一语句） |
| B. 混合族形参不匹配延迟 | `defer_mixed_mismatch` | 实参类型与形参（含 var 声明注解）仅数值族不同（Int64/Float64 互错）且表达式可延续时延迟 | fuzz：`f + 1`（f:Float64 赋给 Int64）等族 |
| C. 字符串拼接锚点 | `should_defer_expression_error` | `string concatenation requires String` 不再跨行延迟到 `}\n`，改在语句换行处 | 官方 err_arith_non_numeric 族锚点在首个不可延续 token |
| E. 索引越界型延迟 | 同上 | `array index must be Int64` 仅在尾部是延续符（`.`/`(`/运算符）时延迟到 `]`；mod/rel/range/lambda/ctor-arg 类错误保持锚定在运算符 | 官方锚点实测：`%`、`<`、`..`、`=>`、`,` 处即时报 |

## 门禁（100/100，与 v7 相同基线）

| 验证项 | 结果 |
|---|---|
| 官网 harness wrong（50 例） | 50/50 PASSED（30s） |
| 官网 harness wrong2（50 例） | 50/50 PASSED（25s） |
| 锚点抽查（9 官方族用例） | 与预期位一致（t_mixed=37/println、t_nb=32/`}`、t_f1v=28/`}`、t_abc1=17/换行、t_idx15=30/`]`、t_sidx15=24/`]` 等） |

## 差分 fuzzer（context 无关 statement 用例 67 个）

- 分歧数：34（v7）→ 31（v8），修复 3 例：
  - Float64+Int64 → Int64 混合族算术：284 → 288 ✓
  - `Int64 && Bool` 逻辑：283 → 286 ✓
  - `arr[Float64]` 索引：286 → 288 ✓
- 位置修正 2 例：`"abc"+1` 290→289、`str[Float64]` 285→287（更接近但未到 gt）
- 剩余 31 例 = oracle 位置偏差（如 arith-non-numeric 官方锚在 LHS 操作数、
  双 token 合并导致 1-off 不可达）+ 容器 API 类（push/put/ctor 族，官方锚在
  arg 尾 `,`/`)`，属无法从单边修正的类别）。
- context_api 类 fuzzer：12 分歧不变（min/max 模型延迟至 `)`，官方即如此），
  validation 50/50。

## 与 v7 对比与预期

- v7 官方 = 60/100（与 v1 持平）。v8 相对 v7 只动了报错时机，未改任何
  合法路径行为；本地 100/100 门禁不回归 ⇒ 不会丢已过的 60 例。
- 预期收益类别：官方隐藏集中若含 same-line 的 mixed-family 算术/逻辑/索引
  用例（err_arith_mixed_family、err_logical_non_bool、err_index_*），v8 将
  从错位/提前报到正确位置；若官方集全是跨语句用例则与 v7 持平。

## 待办

- [ ] 上传 v8 zip，等待官网结果
- [ ] 官网结果回来后按流程存档并对比
